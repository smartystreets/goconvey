package execution

import "testing"

func TestReportSuccesses(t *testing.T) {
	// file := newMemoryFile()
	// printer := newPrinter(file)
	// reporter := NewStoryReporter(printer)

	// firstSuccess := NewSuccessReport()
	// secondSuccess := NewSuccessReport()

	t.Skip("Not sure if this is even worth testing...it's like a UI: fiddly.")
}

// TODO: stories with steps that share the same title (should still indent and print correctly)
// TODO: pass, fail, error
