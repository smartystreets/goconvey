package main

import "testing"

func Test(t *testing.T) {
	Convey("The purpose of this file is to generate some", t, func() {
		Convey("goconvey output to test the goconvey-server script.", nil)
	})
}
